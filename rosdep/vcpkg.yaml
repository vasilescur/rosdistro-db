ftdi-eeprom:
  windows: []
libusb-dev:
  windows:
    vcpkg:
      packages: [libusb-win32]
libftdi-dev:
  windows:
    vcpkg:
      packages: [libftdi]

# rosdep packages
libssl-dev:
  windows:
    vcpkg:
      packages: [openssl]
